[package]
name = "quantum"
version = "0.1.0"
edition = "2024"

[dependencies]
nalgebra = "0.35.0"
num-complex = "0.4"
rustfft = "6"

# Dependencies are compiled optimized even in a debug build, this crate is not.
[profile.dev.package."*"]
opt-level = 3
